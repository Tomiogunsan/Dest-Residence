import React from 'react'
import { useEffect } from 'react'

export default function Contact({userR}) {
    useEffect(()=> {
        async function getLandlord(){
            const docRef = doc(db, 'users')
        }
    }, [])
  return (
    <div>Contact</div>
  )
}
