import React from 'react'
import { useEffect } from 'react'

export default function Contact({userRef, listing}) {
    useEffect(()=> {
        async function getLandlord(){
            const docRef = doc(d, 'users')
        }
    }, [])
  return (
    <div>Contact</div>
  )
}
